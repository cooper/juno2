#!/usr/bin/perl -w
use warnings;
use strict;
my $salt = rand(9001);
print 'Password: ';
my $password = <STDIN>;
chomp $password;
$password = crypt($password,$salt);
print "Hash: $password\n";
print "Salt: $salt\n";
exit;
